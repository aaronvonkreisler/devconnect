import React from 'react';
import { LinearProgress } from '@material-ui/core';

const LinearLoading = () => {
   return (
      <div>
         <LinearProgress />
      </div>
   );
};

export default LinearLoading;
